[project]
name = "web-search-mcp"
version = "0.1.0"
description = "A FastMCP server providing web search capabilities"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.5.0",
    "httpx>=0.27.0",
]

[project.scripts]
web-search-mcp = "main:main"
